<% content_for(:title, 'Request an invitation') %>

<div class="row justify-content-center">
  <div class="col-lg-6">
    <div class="card">
      <div class="card-header text-muted">Request an invitation</div>
      <div class="card-body">
        <%= render 'form', invitation: @invitation %>
      </div>
    </div>
  </div>
</div>
