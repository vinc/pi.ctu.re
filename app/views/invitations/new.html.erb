<% content_for(:title, 'Request an invitation') %>
<% content_for(:page, 'request-invitation') %>

<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="card">
      <h4 class="card-header">Request an invitation</h4>
      <div class="card-body">
        <%= render 'form', invitation: @invitation %>
      </div>
    </div>
  </div>
</div>
