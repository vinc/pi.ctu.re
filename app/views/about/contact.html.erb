<% title t('contact.title') %>

<div class="row">
  <div class="col-lg-3">
    <%= render 'about/nav' %>
  </div>
  <div class="col-lg-6">
    <div class="card">
      <div class="card-header text-muted"><%= t('contact.title') %></div>
      <div class="card-body">
        <%= render_markdown(t('contact.body_markdown', email: email_munging(@email))) %>
      </div>
    </div>
  </div>
</div>
