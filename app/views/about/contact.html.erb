<% content_for(:title, t('contact.title')) %>

<div class="row">
  <div class="col-lg-3">
    <%= render 'about/nav' %>
  </div>
  <div class="col-lg-6">
    <div class="card">
      <h4 class="card-header"><%= t('contact.title') %></h4>
      <div class="card-body">
        <%= t('contact.body_html', email: @email) %>
      </div>
    </div>
  </div>
</div>
