<% pictures.each do |picture| %>
  <%= link_to picture_path(picture, from: @from, order: @order) do %>
    <%= image_tag(picture.image.thumb_url('x300'), alt: picture.alt, size: picture.image.thumb_size('x300')) %>
  <% end %>
<% end %>
